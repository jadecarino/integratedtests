#
# Copyright contributors to the Galasa project
#
# SPDX-License-Identifier: EPL-2.0
#

apiVersion: v1
kind: Service
metadata:
  name: maven-inttests
  labels:
    app: maven-inttests
spec:
  type: ClusterIP
  selector:
    app: maven-inttests
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP