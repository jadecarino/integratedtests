pluginManagement {
    repositories {
        maven {
            url = "$sourceMaven"
        }
        gradlePluginPortal()
    }
}

rootProject.name = "galasa-inttests-parent"

include 'dev.galasa.inttests'
